# js-lab-41
### Lab41 Loop: Lab2
code ทั้งสองอันนี้จะแสดง alert ออกมาเหมือนกันทั้งหมดหรือไม่ ///ไม่เหมือนกันทั้งหมด
```Javascript
let i = 0;
while (++i < 5) {
  alert( i);
}
```

```Javascript
let i = 0;
while (i++ < 5) {
  alert( i);
}
```
