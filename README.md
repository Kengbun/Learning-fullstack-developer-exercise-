# 	Lab18 Object: Guess Result3			
	บรรทัดที่มี * ให้ผลลัพธ์เป็นอะไร เพราะอะไร			
	`````````			
	"let user = {
  name: 'John',
  sayHi: function () {
    console.log(this.name);
  }
};

(user.sayHi)(); //* ผลลัพธ์ John เพราะ เรียกใช้ฟั่งชั่นที่ แสดงค่าของ name ที่เป็น property ของ user
````````	