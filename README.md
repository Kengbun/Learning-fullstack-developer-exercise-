# Lab 13 ES6: result3				
ผลลัพธ์ในบรรทัดที่มี * มีค่าเป็นอะไรและเพราะอะไร				
				
"let numbers = [10, 20, 30];
[numbers[1], numbers[2]] = [numbers[2], numbers[1]];
console.log(numbers); // * [10, 30, 20] เพราะถูกสลับต่ำแหน่ง โดยที่ index1 สลับไป index2, index2 สลับไป index1 
                    "				