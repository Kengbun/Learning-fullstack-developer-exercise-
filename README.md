# Lab 11 ES6: result1			
ผลลัพธ์ในบรรทัดที่มี * มีค่าเป็นอะไรและเพราะอะไร			
			
"let [first, second, third] = ['Maya', 'Marisa', 'Chi'];
console.log(first); // * Maya  เพราะถูกกำหนดค่าตามลำดับ ซึ่ง first คือลำดับที่1 และ Maya ก็ลำดับที่1 จึงถูกจับคู่กัน
console.log(second); // ** Marisa ตำแหน่ง หรือลำดับเหมือนกัน เหตุผลตามข้อแรก
console.log(third); // *** Chi ตำแหน่ง หรือลำดับเหมือนกัน เหตุผลตามข้อแรก                      "			